# Complete bounded repair ports

The paper-facing boundary is the import closure of
`RepairPorts.Gates.CompletePorts`. It covers the coefficient-port object,
intrinsic reconstruction radius, standard-code duality bridge, MDS
reconstruction, pointed transfer, finite reliability, bounded-radius erasure
calculus, and the pointed rank-sum specialization.

The pointed rank-sum theorems take deletion, contraction, and rank identities
as explicit hypotheses. The reliability theorems are finite-sum and polynomial
identities. No generated certificate, native evaluation, executable
enumerator, or project-local mathematical axiom occurs in the gate closure.
The completed cubic-axis transfer theorem takes Singer regularity as an
explicit hypothesis.

The exact 31-module source and target boundaries are content-addressed in
`trust/source-manifests/complete_ports.json` and
`trust/manifests/complete_ports.json`. The terminal declarations and their
expected axiom sets are recorded in `trust/areas/complete_ports.toml`.
